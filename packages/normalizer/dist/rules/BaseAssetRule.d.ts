import { NormalizerRule } from "../engine/NormalizerEngine";
/**
 * Ensures every raw asset gets a base KnowledgeObject structure:
 * - canonical ID
 * - type
 * - title
 */
export declare const BaseAssetRule: NormalizerRule;
